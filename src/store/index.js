import { createApp } from 'vue';
import { createStore } from 'vuex';

const app = createApp({});
const store = createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
});

app.use(store);

export default store;
